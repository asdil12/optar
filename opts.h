#include "optar.h"

extern int paper_format_a4;
extern unsigned paper_dpis;

extern unsigned XCROSSES, YCROSSES, DATA_WIDTH, DATA_HEIGHT, WIDTH, HEIGHT, NARROWWIDTH, NARROWPIXELS, WIDEWIDTH, WIDEPIXELS, REPPIXELS;
extern unsigned long TOTALBITS;
extern unsigned FEC_SYMS, NETBITS, USEDBITS;

void calc_opts(void);
